---
title: "GPT-5.6 Sol"
description: "Call the gpt-5.6-sol text route through SnapGen."
api: "POST /v1/chat/completions"
---

`gpt-5.6-sol` is a distinct GPT-5.6 route with the same public token price as the standard `gpt-5.6` route.

| Property | Value |
| --- | --- |
| Model ID | `gpt-5.6-sol` |
| Input price | $1.75 per 1M tokens |
| Output price | $14.00 per 1M tokens |
| Streaming | Supported with `stream: true` |

<ParamField body="model" type="string" required>
  Set to `gpt-5.6-sol`.
</ParamField>

<ParamField body="messages" type="array" required>
  Ordered system, user, and assistant messages.
</ParamField>

## Request

```bash
curl https://api.snapgen.org/v1/chat/completions \
  -H "Authorization: Bearer $SNAPGEN_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-5.6-sol",
    "messages": [{"role": "user", "content": "Find concurrency risks in this worker design."}]
  }'
```

Read generated text from `choices[0].message.content`. See [Chat Completions protocol](/api-reference/chat-completions) for streaming and response fields.

## Compatible protocol paths

| Format | Path |
| --- | --- |
| Chat Completions | `/v1/chat/completions` |
| Responses | `/v1/responses` |
| Anthropic Messages | `/v1/messages` |
| Gemini native | `/v1beta/models/gpt-5.6-sol:generateContent` |

<Note>
  Compare `gpt-5.6-sol` and `gpt-5.6` on quality and latency. Their current
  public prices are equal, but they remain separate routes.
</Note>
